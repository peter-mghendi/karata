name: Deploy

on:
  push:
    branches: [ "main" ]
    
env:
  DEPLOYS_ENABLED: false

jobs:
  deploy:
    permissions:
      actions: write
      contents: read
      deployments: write

    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Check Deploy Toggle
        run: |
          echo "DEPLOYS_ENABLED: $DEPLOYS_ENABLED"
          if [ "$DEPLOYS_ENABLED" != "true" ]; then
            echo "🚫 Deploys are disabled. Exiting."
            gh run cancel ${{ github.run_id }}
            gh run watch ${{ github.run_id }}
            exit 0
          fi
        env:
          GH_TOKEN: ${{ secrets. GITHUB_TOKEN }}

      - name: Create GitHub Deployment
        id: deployment
        run: |
          deploy=$(gh api repos/${{ github.repository }}/deployments \
            -X POST \
            -F ref="${{ github.sha }}" \
            -F task="deploy" \
            -F auto_merge=false \
            -F environment="production" \
            -F description="Deploying commit ${{ github.sha }}" \
            -F transient_environment=false \
            -F production_environment=true \
            -f required_contexts\[\] \
            --jq '.id')
          echo "deploy=$deploy" >> $GITHUB_ENV     
          echo "Created deploy $deploy"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify on Deployment
        run: |
          next=$(git rev-parse --short $GITHUB_SHA)
          echo "next=$next" >> $GITHUB_ENV     
          echo "Deploying commit $next"

      - name: Mark Deployment as In Progress
        run: |
          gh api repos/${{ github.repository }}/deployments/${{ env.deploy }}/statuses \
            -X POST \
            -F state="in_progress" \
            -F description="Deployment started" \
            -F environment_url="https://karata.app"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install Android workload
        run: dotnet workload install android

      - name: Restore dependencies
        run: dotnet restore Karata.slnf

      - name: Build
        run: dotnet build --no-restore Karata.slnf

      - name: Test
        run: dotnet test --no-build --verbosity normal Karata.slnf

      - name: Publish
        run: dotnet publish -c Release -o app Karata.slnf

      - name: Upload files via SCP
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          source: "app/*"
          target: "/opt/karata/${{ env.next }}"
          strip_components: 0

      - name: Restart application service
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: sudo /usr/local/bin/deploy_karata.sh "/opt/karata/${{ env.next }}"

      - name: Mark Deployment as Success
        if: success()
        run: |
          gh api repos/${{ github.repository }}/deployments/${{ env.deploy }}/statuses \
            -X POST \
            -F state="success" \
            -F description="Deployment completed successfully" \
            -F environment_url="https://karata.app"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Mark Deployment as Failure
        if: failure()
        run: |
          gh api repos/${{ github.repository }}/deployments/${{ env.deploy }}/statuses \
            -X POST \
            -F state="failure" \
            -F description="Deployment failed" \
            -F environment_url="https://karata.app"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}