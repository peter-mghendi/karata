name: Publish Karata.Web

on:
  push:
    branches: [ "main" ]

jobs:
  release:
    permissions:
      contents: write

    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        
      - name: Set version
        id: params
        run: echo "version=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT  

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install Webassembly workload
        run: dotnet workload install wasm-tools

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal
        
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}  

      - name: Publish static site
        run: |
          dotnet publish --configuration Release -o dist src/Karata.Web/Karata.Web.csproj
          tar -czf karata-web.tar.gz dist/wwwroot
      
      - name: Deploy to releases-karata-web
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: releases-karata-web
          commit-message: "Release ${{ steps.params.outputs.version }}"
          folder: dist/wwwroot
          force: true
          single-commit: true
        
      - name: Attach assets to release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false
          tag_name: ${{ steps.params.outputs.version }}
          files: karata-web.tar.gz